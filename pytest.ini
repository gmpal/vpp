[pytest]
pythonpath = .

markers =
    unit: tests that run without any external infrastructure (DB, Kafka, network)
    integration: tests that require a live TimescaleDB/PostgreSQL instance

addopts = -ra
