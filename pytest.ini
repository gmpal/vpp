[pytest]
pythonpath = .
testpaths = tests

markers =
    unit: runs with no database, Kafka, network or .env (assigned automatically to unmarked tests)
    integration: needs the disposable test database from docker-compose.test.yaml (make test-db-up)

addopts = -ra
