# Research data pipeline — Fluvius meter data ingestion for VPP digital twin
#
# build_packs.py  — one-time pre-packager: reads raw Fluvius CSVs, creates
#                   community packs (stratified sample per size/seed).
# load_pack.py    — bootstrap: loads a single pack into the database.
